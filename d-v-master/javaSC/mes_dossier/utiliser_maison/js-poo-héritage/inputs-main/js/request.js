'use strict'

  class Request {
               
        getInputs(elements) {
            for (let i = 0; i < elements.length; i++) {
                console.log(elements[i].value);

            }
        }
    }
    

    export default {Request};
